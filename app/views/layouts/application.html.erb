<!DOCTYPE html>

<html>

  <head>
    <title> Jarvis | <%= @titre %></title>
	<meta charset="utf-8">
	<meta name="author" content="Morgann GERARD">
	<%= stylesheet_link_tag "application", media: "all" %>
	<%= stylesheet_link_tag "w3", media: "all" %>
	<%= stylesheet_link_tag "https://fonts.googleapis.com/css?family=Raleway", media: "all" %>
    <%= csrf_meta_tag %>
  </head>

  <body class="w3-light-grey">

	<!-- w3-content defines a container for fixed size centered content, and is wrapped around the whole page content-->
	<div id="container" class="w3-content">

  		<%= render "pages/header" %>

		<!-- Main block in prevision of responsive design -->
		<div id="Main" class="w3-row">

			<!-- Interface block -->
			<div id="interface" class="w3-col l8 s12 w3-container w3-blue w3-round-large">
    			<%= yield %>
    		</div>
    		<!-- json resitution -->
    		<div id="json" class="w3-col l4 s12 w3-container w3-yellow w3-round-large">
				<%= render "pages/json" %>
			</div>

		</div>

		<%= render "pages/footer" %>

	</div>
  </body>
</html>